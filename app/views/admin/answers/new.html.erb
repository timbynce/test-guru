<div class="row justify-content-center mt-3">
  <div class="col col-md-6">
    <h1>New Answer</h1>

    <p>
      <%= @question.body %>
    </p>

    <%= render 'form', answer: @answer %>
    <%= link_to t('back', scope: 'helpers.submit'), admin_question_path(@question), class: 'btn btn-primary mt-1' %>
  </div>
</div>
