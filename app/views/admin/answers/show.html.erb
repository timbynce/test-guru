<div class="row justify-content-left mt-3">
  <div class="form-group">
    <h3><%= t('.question') %></h3>

    <p>
      <%= @answer.question.body %>
    </p>

    <h3><%= t('.answer') %></h3>

    <p>
      <%= @answer.body %>
    </p>

    <p>
      <b><%= t('.correct') %></b>
      <%= @answer.correct %>
    </p>

    <%= link_to t('back', scope: 'helpers.submit'), admin_question_path(@answer.question), class: 'btn btn-primary mt-1' %>
  </div>
</div>
