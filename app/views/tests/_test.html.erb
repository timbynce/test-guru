<tr>
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.questions.count %></td>
  <td><%= button_to t(".start"), start_test_path(test), class: 'btn btn-outline-primary mt-1' %></td>
</tr>
