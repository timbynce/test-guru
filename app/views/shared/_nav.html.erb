<nav class="navbar navbar-light bg-light">
  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
  <span class="navbar-text">

    <% if user_signed_in? %>
      <p class="nav_user">
      <%= t(".welcome", first_name: current_user.first_name, last_name: current_user.last_name) %>

      <% if current_user.is_a?(Admin) %>
        <p> 
          <%= link_to t('helpers.admin_panel'), admin_tests_path %> |
          <%= link_to t('admin.nav.feedback'), new_feedback_path %> |
          <%= link_to t(".logout"), destroy_user_session_path, method: :delete%>
        </p>
      <% else %>
        <p> 
            <%= link_to t('.feedback'), new_feedback_path %> |
            <%= link_to t(".logout"), destroy_user_session_path, method: :delete%> 
        </p> 
      <% end %>
      </p>
    <% else %>
      <p class="nav guest"> <%= link_to t('helpers.sign_in'), new_user_session_path, method: :get %> | <%=link_to t('helpers.registration'), new_user_registration_path, method: :get%> </p>
    <% end %>
  </span>
</nav>
