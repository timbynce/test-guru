<nav class="navbar navbar-light bg-light">
  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
  <span class="navbar-text">

    <% if user_signed_in? %>
      <p class="nav_user">
      <%= t(".welcome", first_name: current_user.first_name, last_name: current_user.last_name) %>

      <% if current_user.is_a?(Admin) %>
        <p> 
          <%= link_to t(".logout"), destroy_user_session_path, method: :delete%> |
          <%= link_to 'Admin Panel', admin_tests_path %>
        </p>
      <% else %>
        <p> <%= link_to t(".logout"), destroy_user_session_path, method: :delete%> <p> 
      <% end %>
      </p>
    <% else %>
      <p class="nav guest"> <%= link_to 'Sign in', new_user_session_path, method: :get %> | <%=link_to 'Registration', new_user_registration_path, method: :get%> </p>
    <% end %>
  </span>
</nav>
